<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".Activities.ViewJourneyPlannerActivity"
    android:id="@+id/drawer_layout">

    <LinearLayout
        android:orientation="vertical"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="test.collegecarpool.alpha.Activities.FindCarpoolActivity"
        android:weightSum="1">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:height="50dp"
            android:outlineProvider="none"
            android:id="@+id/carpool_search_bar"/>

        <ListView
            android:paddingTop="10dp"
            android:layout_width="match_parent"
            android:layout_height="320dp"
            android:id="@+id/find_carpool_listview"
            android:longClickable="true"
            tools:layout_editor_absoluteY="8dp"
            tools:layout_editor_absoluteX="8dp">
        </ListView>

        <Button
            android:id="@+id/carpool_search_button"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:height="80dp"
            android:text="Search"
            android:gravity="bottom|center"
            android:textAlignment="gravity"
            android:layout_weight="0.18" />

    </LinearLayout>

    <android.support.design.widget.NavigationView
    android:id="@+id/nav_view"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:background="@color/bg_main"
    app:itemTextColor="@color/white"
    app:itemIconTint="@color/white"
    app:menu="@menu/nav_menu"
    android:layout_gravity="start">
    </android.support.design.widget.NavigationView>

</android.support.v4.widget.DrawerLayout>